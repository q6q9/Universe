#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <unordered_map>

using namespace std;


int main(){
        vector<int> a;
        a.push_back(5);
        a.push_back(1);
        a.push_back(-432);
        unordered_map<string, 
        getchar();
}